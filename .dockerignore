# Exclude everything by default, then whitelist what the build needs
*

# Node source
!src/
!src/**
!package.json
!package-lock.json

# Don't send test files, docs, or dev configs into the build context
node_modules
output
logs
checkpoints
coverage
.git
.env
.env.*
*.log
tests/
docs/
*.md
